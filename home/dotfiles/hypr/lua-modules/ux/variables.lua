-- --- [ i ] - Variables -
local mainMod = "SUPER"

local terminal = "{{@@ terminal @@}}"
local browser = "{{@@ browser @@}}"
local file_manager = "{{@@ file_manager @@}}"
local volume_control = "{{@@ volume_control @@}}"

return {
    mainMod = mainMod,
    terminal = terminal,
    browser = browser,
    file_manager = file_manager,
    volume_control = volume_control
}
